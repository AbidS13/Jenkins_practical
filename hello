pipeline{
    agent any

    stages{
        stage('Build'){
            steps{
                echo "Building first stage---Build"
            }
        }
        stage('Run'){
            steps{
                echo "Running the second stage--RUN"
                bat "node simple.js"
            }
        }
    }
}
